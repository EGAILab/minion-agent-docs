# CE-L13-WP131-01 Lane E — independent R010 revision 4 re-review

## Verdict

```text
R010 CHARACTERIZATION
    APPROVED

OWNER DECISION READY
    YES

Python/Rust Layer 13 implementation
    NOT AUTHORIZED
```

Revision 4 closes the remaining blocking portion of `L13-WP131-R010-D`. The characterization now
provides two coherent, implementable, unselected owner options for model-visible error text. No
active Lane-E parity, uncertainty, or contract-assurance finding remains.

## Exact review target

| Item | Exact state |
|---|---|
| Coordination | `EGAILab/minion-agent#48`, `STATUS = CONTRACT_CONVERGENCE`, `NEXT_OWNER = Codex` |
| Docs PR | `EGAILab/minion-agent-docs#132` @ `51120049fe6119cd753ccbcb91143c6fcc2136c7` |
| Artifact | `assurance/layers/13-wp131-ce-l13-wp131-01-r010-error-projection.md` |
| Frozen manifest | `EGAILab/minion-agent#52` @ `cca8d8b325bb159be549e10c8321b3468f043e7f` |
| Pinned Pi | `b7bb00b936dbe21b8e160b3e89efdec361846699` |

The coordination issue was open and valid. Both candidate PR heads were open, Ready for Review,
and remote-reachable at the exact recorded SHAs. This was a Lane-E-only review; R006, R007,
WP-12.E1, and existing Layer-12 semantics were not reopened.

## Independent verification

Rechecked the revision-4 delta against:

- pinned Pi `ls.ts` and `read.ts` error sites;
- certified `FsErrorCode` and provider-neutral filesystem operations in `spec/execution.md`;
- approved WP-12.E1 `list_dir_raw`/`probe_dir_entry` behavior; and
- the certified generated-tool-error result shape in `spec/tools.md` and both implementations.

Confirmed:

1. The specific “addressed path is a directory” content-read subcase maps only to
   `is_directory`.
2. The general later `read_text_file`/`read_binary_file` failure site independently covers
   `not_found`, `permission_denied`, `not_directory`, `invalid`, `not_supported`, and `unknown`.
3. `list_dir_raw -> not_supported` remains a projected whole-operation failure.
4. Individual `probe_dir_entry` failures remain silent/skipped and are not assigned tool-level
   error text.
5. R010-B uses `FsErrorCode` only internally to select text. Both options preserve the certified
   generated-error `details: {}` shape.
6. The inventory consistently identifies four stable templates: three `ls` templates plus the
   shared abort template.

## Finding closure ledger

| Finding | Final result | Status |
|---|---|---|
| `L13-WP131-R010-A` | Genuine owner matrix replaces the formerly ungoverned single resolution. | `RESOLVED` |
| `L13-WP131-R010-B` | Ordinary raw/hybrid taxonomy is no longer collapsed. | `RESOLVED` |
| `L13-WP131-R010-C` | No nonexistent structured-error channel is promised; details remains `{}`. | `RESOLVED` |
| `L13-WP131-R010-D` | Whole-operation, skipped-probe, and complete later-read mappings are all explicit. | `RESOLVED` |
| `L13-WP131-R010-E` | Stable template count is consistently four. | `RESOLVED` |

## Owner decision matrix

The characterization is ready for the owner to choose one option explicitly:

### R010-A — preserve Pi's error-text structure

- Preserve the four stable Pi-authored templates verbatim.
- On raw/hybrid sites, surface the underlying provider error message using the same raw/wrapper
  structure Pi uses.
- Maximum structural fidelity, with inherited platform/provider-dependent text.
- Generated error details remain `{}`.

### R010-B — normalize raw/hybrid text only

- Preserve the same four stable Pi-authored templates verbatim.
- On raw/hybrid sites only, use `FsErrorCode` internally to select a deterministic Layer-13 text
  template.
- Explicit intentional divergence: exact provider/OS wording is lost and no structured error code
  is exposed in the final result.
- Generated error details remain `{}`.

Neither option is selected by this review. If R010-B is selected, the exact closed message
templates still must be written into the normative contract and reviewed before implementation;
the owner decision authorizes the normalization policy/divergence, not unspecified implementation
wording.

## Contract-quality answers

| Question | Answer |
|---|---|
| Is the pinned-Pi mixed authored/raw/hybrid structure accurately characterized? | Yes. |
| Does either option silently change the certified tool-result shape? | No. |
| Does the mapping cover provider-neutral read failures, including `not_supported`? | Yes. |
| Does it distinguish projected whole-operation errors from skipped probe errors? | Yes. |
| Are both options implementable independently in Python and Rust? | Yes. |
| Is an explicit owner choice still required? | Yes. |
| Does approval authorize implementation? | No. |

## Findings by taxonomy

```text
PI_BEHAVIOR_UNCERTAIN
    none

PI_PARITY_DEFECT
    none

CONTRACT_ASSURANCE_DEFECT
    none

PARITY_NEUTRAL_HARDENING
    none

PARITY_CONSTRAINED_RISK
    none
```

## Required next action

Move issue #48 to `BLOCKED_FOR_OWNER`. The owner must explicitly select `R010-A`, `R010-B`, or a
scoped alternative. Do not infer a choice. This approval does not authorize Python or Rust Layer
13 implementation, a Layer-12 change, WP-12.E1 work, or Layer 14.
